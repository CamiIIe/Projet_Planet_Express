<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projet_planet_express">
    <!-- Pour la localisation de l'utilisateur -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">

        <!-- Pour les services Google Play -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" /> <!-- Pour l'API Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/maps_api_key" />

        <activity android:name=".Activities.AjoutTrajetActivity"></activity>
        <activity android:name=".Activities.AfficheItineraireActivity"></activity>
        <activity android:name=".Activities.ParametresActivity" />
        <activity android:name=".Activities.PrincipalActivity">

        </activity>
        <activity android:name=".Activities.Planet_Express_Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.InscriptionActivity"></activity>
        <activity android:name=".Activities.ConnexionActivity">
        </activity>
    </application>

</manifest>